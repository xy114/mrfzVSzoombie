# 游戏基础设定

## 1. 植物信息完整性（高优先级）

每个植物的信息展示必须包含**生命值**、**攻击力**、**部署费用**。其他信息（如攻击间隔、技能冷却、特殊效果等）酌情添加。在 PlantConfig、图鉴、战斗卡片等所有展示界面中均需体现。

## 2. 植物尺寸系统（高优先级）

- 每个植物的虚影和实际渲染尺寸由 Plant 基类的 `getPlacementParams()` 统一提供（bodyType、renderSize、aspectRatio）。
- 三级渲染（**网格虚影**、**拖拽虚影**、**实际放置**）必须通过同一入口 `Lawn.getPlacementRect()` 获取位置和尺寸，禁止在任何渲染路径中硬编码植物尺寸参数。
- **实际形象和网格虚影的大小和位置必须相同。**
- **拖拽虚影和网格虚影的大小必须相同。**
- 新增植物时，只需在 PlantConfig 的 combat 中声明 bodyType，其余自动继承基类默认值（renderSize=80, aspectRatio=1.0）。

## 3. 时间流速操作规范（高优先级）

所有涉及时间流速的临时操作（时停、面板减速等）必须使用乘/除模式——开始时 `timeScale *= 因子`，结束时 `timeScale /= 因子`。禁止硬编码 `setTimeScale(1.0)` 来恢复时间。速度倍率按钮同样使用 toggle 乘除（`*= 2` / `/= 2`），确保多个时间操作叠加时互不覆盖。

## 4. 描述文本规范（高优先级）

所有 description、skillDescription 等文本字段不得包含 emoji 字符，保持纯文字描述。

## 5. 时停时长倍速适配（高优先级）

时停技能的实际时长（setTimeout 延迟）必须除以当前速度倍率（`speedMult`），使得 2 倍速下时停时长减半，确保倍速按钮影响所有时间相关行为。

## 6. 血条/技力条/子弹位置绑定（高优先级）

血条、技力条、子弹生成位置等 UI 元素必须与实际形象的位置绑定，使用 `this.getRenderSize()` 和 `this.x + this.width` 计算，不得硬编码偏移量（如 `+30`）或使用 `CELL_WIDTH` 计算条宽。

## 7. 描述信息完整性（高优先级）

所有植物的 description 和 skillDescription 必须包含关键数值和机制信息（如伤害百分比、伤害公式、持续时间、冷却时间等），确保玩家仅通过阅读描述就能了解植物的完整机制。

## 8. 动态描述系统（高优先级）

植物的 description 和 skillDescription 由 `getFormattedDescription(def)` / `getFormattedSkillDescription(def)` 动态生成，从 `combat` 字段读取数值自动填充。修改 combat 数值后所有界面（图鉴、卡片、面板）自动同步，无需手动更新描述文本。仅衍生皮肤（如维什戴尔、火陈）可保留手写描述。

## 9. 图鉴卡面组成（高优先级）

图鉴中每张植物/敌人卡面由三层叠加组成，按绘制顺序：

1. **立绘层**（最底层）—— 植物或敌人的 GIF/肖像，缩放适配卡面透明窗口
2. **卡面基底**（中层）—— 固定 PNG 素材（`Almanac_PlantCard.png` 316×473 或 `Almanac_ZombieCard.png` 319×490），含相框与文本区，透明窗口露出底层立绘
3. **信息层**（最顶层）—— Canvas 绘制文字：名称、星级、威胁标签、晶核价格等

**未解锁/未遭遇状态**：立绘层应用 `grayscale(0.7)` 去饱和滤镜；卡面基底完全一致不修改；在卡面基底之上叠加整卡统一的黑色半透明膜 `rgba(0,0,0,0.5)`，确保立绘、相框、文本区均匀压暗。文字使用冷调深灰 `#3b414a` 与解锁态暖棕 `rgb(89,32,8)` 区分。

**新增卡面类型时必须严格遵循此三层结构，禁止跳层或新增硬编码分支。**
