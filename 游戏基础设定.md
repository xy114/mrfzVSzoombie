# 游戏基础设定

## 角色系统树状图

```
角色 (Character)
│
├── 基本信息
│   ├── 唯一ID (id)                 — 如 "peashooter", "normal"
│   ├── 显示名称 (name)              — 如 "豌豆射手", "普通僵尸"
│   ├── 阵营 (category)              — plant / enemy / visitor
│   ├── 体型 (bodyType)             — plant / humanoid
│   ├── 渲染宽度 (width)             — plant=100, humanoid=125, imp=60
│   ├── 描述文本 (description)
│   │   → 详情面板（选择页 & 战斗中）
│   │   → 皮肤预览弹窗
│   │   → 卡面
│   ├── 解锁条件 (unlockLevel)
│   │   → 选择页（锁定/解锁状态）
│   │   → 图鉴
│   └── 表情符号 (emoji)
│       → 卡面
│       → 详情面板标题
│
├── 皮肤
│   │
│   │  【每个我方单位都有原皮，衍生皮为可选项】
│   │  【敌方单位只有原皮，切换外观 = 切换单位类型】
│   │
│   ├── 原皮 (default, category: "original")
│   │   │
│   │   ├── 立绘 (portrait)
│   │   │   → 详情面板大图（选择页 & 战斗中）
│   │   │   → 展示选择器 (display picker) 缩略图
│   │   │   → 皮肤预览弹窗大图
│   │   │   → 卡面缩略图 (无大头像时降级为立绘)
│   │   │   → 出战时选择皮肤的小窗格
│   │   │
│   │   ├── 战斗形象 (combat)
│   │   │   → 战斗中 render() 绘制
│   │   │   → 皮肤预览弹窗右下角小战斗图
│   │   │
│   │   ├── 大头像 (headshot)
│   │   │   → 出战槽圆形头像 (deck slot)
│   │   │   → 卡片小缩略图 (preferHeadshot=true 时优先)
│   │   │   → 无独立大头像时降级为立绘
│   │   │
│   │   ├── GIF 动画
│   │   │   → 战斗中动态循环 (闲置/攻击/走路)
│   │   │   → 死亡动画 (仅敌方: DeathEffect 播放)
│   │   │
│   │   ├── 技能
│   │   │   → 战斗中主动/被动触发
│   │   │   → 详情面板技能描述区（选择页 & 战斗中）
│   │   │   → 皮肤预览弹窗技能描述
│   │   │   → 技能无则为"空技能"（显示「暂时还没有技能，敬请开发」）
│   │   │
│   │   └── 卡片图 (card)
│   │       → 选择页面单位卡片 (选择页)
│   │
│   └── 衍生皮 (category: "derived", 付费/解锁，目前仅有 peashooter→wishadel)
│       ├── 立绘 (portrait)           → 同上原皮所有立绘区域
│       ├── 战斗形象 (combat)          → 同上原皮所有战斗形象区域
│       ├── 大头像 (headshot)          → 同上原皮所有大头像区域
│       ├── GIF 动画                  → 战斗中循环 (目前植物衍生皮共用原皮GIF)
│       ├── 技能                      → 替换原皮技能，效果/描述/投射物/伤害类型均不同
│       │                              → 调用区域同原皮技能
│       │                              → 伤害类型在描述文本和 combat.skillDamageType 中同时声明，保持一致
│       ├── 属性加成 (attackBonus等)   → 详情面板攻击力数值显示（选择页 & 战斗中）
│       ├── 价格 (cost)               → 皮肤列表 / 皮肤预览弹窗
│       └── 拥有状态 (owned)          → 皮肤列表 / 皮肤预览弹窗（购买/装备按钮）
│
└── 其他信息
    │
    ├── 星级 (1-3)
    │   → 详情面板（选择页 & 战斗中）
    │   → 升级面板（选择页升星按钮区域）
    │   → 战斗中数值乘算 (healthMult / damageMult / cooldownMult)
    │
    ├── 基础战斗属性
    │   ├── 生命值 (health)            → 详情面板（选择页 & 战斗中）/ 战斗HUD血条
    │   ├── 物理伤害 (damage)          → 详情面板（选择页 & 战斗中）
    │   ├── 防御 (defense)             → 详情面板（选择页 & 战斗中）
    │   ├── 法术抗性 (magicResist)     → 详情面板（选择页 & 战斗中）
    │   └── 速度 (speed, 仅敌方)       → 详情面板（选择页 & 战斗中）
    │   └── 全部属性受星级乘算影响
    │
    ├── 费用/冷却
    │   ├── 阳光消耗 (cost)            → 卡面正面 / 详情面板（选择页 & 战斗中）
    │   └── 冷却时间 (cooldown)        → 卡面正面 / 详情面板（选择页 & 战斗中）
    │   └── 攻速 (shootInterval)      → 详情面板（选择页 & 战斗中）
    │
    ├── 技能详情
    │   ├── 技能描述（skillDescription）   → 详情面板（选择页 & 战斗中）/ 皮肤预览弹窗
    │   │   【所有技能数值（伤害、冷却、持续、范围、伤害类型）必须写进此描述文本】
    │   │   【不再有独立的"技能伤害""技能冷却"等数值行，全部整合入描述】
    │   │   【格式示例：「造成50法术伤害，冷却10s」】
    │   └── 被动技能描述（passiveSkillDescription，仅异客）→ 同上
    │
    └── 解锁与升级
        ├── 解锁状态                   → 选择页卡面 / 关卡选择页
        ├── 升星消耗 (晶核)            → 详情面板升星区（选择页 & 战斗中）
        │                              → 升级面板
        └── 升星收益 (属性倍率)         → 详情面板（选择页 & 战斗中）
```

---

## 皮肤系统规格

### 皮肤分类

| 类型 | category | 说明 | 适用对象 |
|------|----------|------|---------|
| 原皮 | `"original"` | 每个单位的默认外观，id 固定为 `"default"`，始终拥有 | 所有我方单位 + 敌方单位 |
| 衍生皮 | `"derived"` | 付费/解锁的替代外观，可替换技能、属性加成 | 仅我方单位 |

- 敌方单位（僵尸）只有原皮，切换外观通过切换单位类型（normal/cone/shield/imp）实现
- 原皮也是一种皮肤——点击原皮应弹出预览弹窗，和衍生皮行为一致

### 皮肤定义结构

```js
{
  id: 'default',              // 原皮固定 "default"，衍生皮如 "wishadel"
  name: '原皮',               // 显示名
  emoji: '🫛',                // 表情
  description: '...',         // 描述
  skillDescription: '...',    // 技能描述（无技能则为空字符串 ""）
                              //   所有技能数值（伤害、冷却、持续、范围、类型）必须写在此文本中
                              //   格式示例：「造成50法术伤害，灼烧周围3×3格，冷却10s」
  category: 'original',       // 'original' | 'derived'
  cost: 0,                    // 0 = 免费/默认拥有
  owned: true,                // 原皮始终 true

  // 战斗数值（仅运行时需要的数值，展示用数值全在 skillDescription 中）
  combat: {
    attackBonus: 0,
    skillDamage: null,        // null = 使用角色默认技能
    skillCooldown: null,
    shellSpeed: null,
    aimDuration: null,
    explosionRadius: null,
  }
}
```

### 资源键命名规范

```
{unitId}_skin_{skinId}_{resourceType}

resourceType:
  _portrait    — 立绘（详情面板、展示选择器、皮肤预览、卡面缩略图兜底）
  _combat      — 战斗形象（战斗中 render()、皮肤预览小战斗图）
  _headshot    — 大头像（出战槽、小卡片，无则降级为 _portrait）

示例：
  peashooter_skin_default_portrait     — 豌豆射手原皮立绘
  peashooter_skin_default_combat       — 豌豆射手原皮战斗形象
  peashooter_skin_default_headshot     — 豌豆射手原皮大头像
  peashooter_skin_wishadel_portrait    — 维什戴尔立绘
  peashooter_skin_wishadel_combat      — 维什戴尔战斗形象
  peashooter_skin_wishadel_headshot    — 维什戴尔大头像
  normal_skin_default_portrait         — 普通僵尸原皮立绘
  normal_skin_default_combat           — 普通僵尸原皮战斗形象
```

- GIF 动画 key（如 `normal`, `peashooter`）不受此规范影响，属于 GIF 动画系统
- 原皮资源键可指向与旧键相同的文件

### 皮肤配置合并

当前 `SKIN_DEFS`（PlantConfig.js，展示信息）和 `SKIN_CONFIG`（constants.js，战斗数值）分离。统一后每个皮肤定义同时包含展示和战斗两方面，合并入 SKIN_DEFS。

### 存储模型

```
// 我方单位皮肤装备（植物 + 访客）
saveData.equippedSkins[unitId] = skinId   // 默认 "default"

// 主页展示单位
saveData.displayUnit = unitId             // 如 "sunflower", "peashooter"
// displaySkin 删除——展示时直接用该单位的 equippedSkins[unitId]

// 已拥有的衍生皮
saveData.ownedSkins[unitId] = [skinId, ...]
```

---

## 单位渲染尺寸标准

| 体型 (bodyType) | 渲染宽度 (`this.width`) | `getRenderSize()` | 说明 |
|:---|:---|:---|:---|
| `'plant'` | **100** (= `CELL_WIDTH`) | 80 | 植物单位，用于草坪布局和碰撞 |
| `'humanoid'` | **125** | 80 | 人类/人形单位，用于草坪布局和碰撞 |

- `this.width`：实际渲染时的包围盒宽度，`render()` 方法绘制以此为基准
- `getRenderSize()`：给血条布局、草坪部署位计算的参考尺寸
- 高度由子类根据资源宽高比自动计算（如僵尸 GIF 通过 `initAnimators()` 等比缩放）
- 小鬼僵尸 `this.width = 60`（不适用 humanoid 的 125）

---

## 单位分类体系

```
战斗单位
├── 我方单位
│   ├── 植物 (bodyType: 'plant', width: 100)
│   │   ├── 向日葵 (Sunflower)
│   │   ├── 豌豆射手 (Peashooter)
│   │   ├── 坚果 (Nut)
│   │   └── 樱桃炸弹 (CherryBomb)
│   └── 人类 (bodyType: 'humanoid', width: 125)
│       ├── 异客 (category: 'visitor')
│       │   └── 武士零 (KatanaZero)
│       └── 人形皮肤（维什戴尔豌豆射手）
│           └── 属于 plants 数组，但 getBodyType() 返回 'humanoid'
└── 敌方单位
    └── 人类 (bodyType: 'humanoid', width: 125)
        ├── 普通僵尸 (NormalZombie)
        ├── 路障僵尸 (ConeZombie) — 血量 < 30% 时装备破损，变为普通僵尸
        ├── 持盾僵尸 (ShieldZombie) — 血量 < 30% 时装备破损，变为普通僵尸
        └── 小鬼僵尸 (ImpZombie) — width: 60
```

---

### 全部单位明细

| 分类 | 类名 | ID | bodyType | width | 所在数组 | 皮肤 |
|:---|:---|:---|:---|:---|:---|:---|
| 植物 | Sunflower | sunflower | plant | 100 | Game.plants | 原皮 |
| 植物 | Peashooter | peashooter | plant | 100 | Game.plants | 原皮 + wishadel |
| 植物 | Nut | nut | plant | 100 | Game.plants | 原皮 |
| 植物 | CherryBomb | cherrybomb | plant | 100 | Game.plants | 原皮 |
| 植物(皮肤) | Peashooter (wishadel) | peashooter | **humanoid** | **125** | Game.plants | wishadel |
| 异客 | KatanaZero | katana_zero | humanoid | 125 | Game.visitors | 原皮 |
| 僵尸 | NormalZombie | normal | humanoid | 125 | Game.zombies | 原皮 |
| 僵尸 | ConeZombie | cone | humanoid | 125 | Game.zombies | 原皮 |
| 僵尸 | ShieldZombie | shield | humanoid | 125 | Game.zombies | 原皮 |
| 僵尸 | ImpZombie | imp | humanoid | **60** | Game.zombies | 原皮 |

---

### 关键区分

| 维度 | 植物 (plant) | 异客 (visitor) | 人形皮肤 | 僵尸 (enemy) |
|------|-------------|---------------|---------|-------------|
| 所在数组 | `Game.plants` | `Game.visitors` | `Game.plants` | `Game.zombies` |
| bodyType | `'plant'` | `'humanoid'` | `'humanoid'` | `'humanoid'` |
| 受 timeScale 影响 | 是 (scaledDelta) | 否 (deltaTime) | 是 (scaledDelta) | 是 (scaledDelta) |
| 撤退按钮图标 | `retreat_plant` | `retreat_humanoid` | `retreat_humanoid` | - |
| 渲染宽度 | 100 | 125 | 125 | 125 (小鬼60) |
| 皮肤系统 | 有 (原皮+衍生皮) | 有 (仅原皮) | 有 | 无 (切换外观=切换单位类型) |

---

### 单位生命周期

1. **部署**：拖拽卡片至草坪格子 → `handleDrop()` → `addPlant()` / `addVisitor()` + `lawn.plant()`
2. **正常死亡**：生命值归零 → `alive = false` → `_tickRetreatTimers()` 清理网格并移出数组
3. **主动撤退**：点击信息面板撤退按钮 → `retreatUnit()`（返还50%阳光，设 `_retreating = 300`）→ 撤退动画（缩小淡出，300ms）→ `_tickRetreatTimers()` 清理

---

### 主动撤退返还规则

- 返还种植消耗的 50% 阳光（仅植物，异客无消耗）
- 返还数值从单位位置飞向阳光计数器
- 生命耗尽撤退不返还阳光

---

### GIF 渲染规则

- GIF 资源通过 `window.__gifManifest`（`index.html` 内联脚本解析）预加载帧数据
- 仅在进入战斗时调用 `assetManager.loadGifManifest()` 注入帧缓存
- 非战斗场景使用 `assetManager.getImage()` 返回静态首帧图（用于立绘、图鉴等）
- 渲染遵循「优先等比例缩放至相同宽度」原则：`drawWidth = targetWidth`，`drawHeight = naturalHeight × (targetWidth / naturalWidth)`

---

### 技能冷却分类

技能按冷却启动时机分为两种类型：

| 类型 | 说明 | 冷却启动时机 | 例子 |
|------|------|-------------|------|
| **脱手技** | 发射投射物后立刻开始冷却 | 子弹/导弹脱离单位时 | 豌豆射手火焰豌豆、维什戴尔追踪爆弹 |
| **固定技** | 技能动作/效果结束后才开始冷却 | 技能持续时间结束时 | 坚果护甲强化、武士零时停斩（主动+被动） |

脱手技的冷却 = 发射间隔，固定技的冷却 = 技能结束后到下次可用之间的等待时间。
